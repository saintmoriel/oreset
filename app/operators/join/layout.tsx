import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join Operator Cohort | Oreset',
  description:
    'Apply to Oreset’s African native language operator cohorts for Foundry training, certification, and AI review placement.',
}

export default function OperatorsJoinLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
