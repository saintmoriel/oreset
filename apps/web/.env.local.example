NEXT_PUBLIC_API_URL=http://api.oreset.local:4000

# Must match apps/api's ACCESS_TOKEN_SECRET exactly — middleware.ts verifies
# the same JWT signature the API signs with.
ACCESS_TOKEN_SECRET=replace-with-the-same-secret-as-apps-api-ACCESS_TOKEN_SECRET
