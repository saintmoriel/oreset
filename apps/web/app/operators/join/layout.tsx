import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join Reviewer Cohort | Oreset',
  description:
    "Apply to Oreset's African native language reviewer cohorts for training, certification, and AI review placement.",
}

export default function OperatorsJoinLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
